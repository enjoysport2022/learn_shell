#!/usr/bin/env bash

#Background Processes
#The simplest way to start a background process is to add an ampersand (&) at the end of the command.

#Listing Running Processes
#ps(process status): see your own processes
#-f: f for full

#Stopping Processes
#前端进程: CTRL + C
#后端进程: ps获取pid, 然后使用kill命令
#kill pid
#kill -9 pid: 强制杀死该进程

#top命令
#quickly showing processes sorted by various criteria
#shows information about physical and virtual memory, CPU usage, load averages, and your busy processes

